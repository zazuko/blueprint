export interface DashGroupViewerData {
    label: string;
}

/*

export interface DashComponent {
  component: Type<any>;
  data?: any;
  viewer?: FluxViewer;
  order: number;
  // if > zero, this indicates that the data shown by the component is incomplete, since it was limited by the LIMIT clause in the query
  dataLimitedTo: number;
}
*/